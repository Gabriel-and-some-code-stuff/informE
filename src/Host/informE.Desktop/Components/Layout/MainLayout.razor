@inherits LayoutComponentBase

@*
    Layout de reserva: e o DefaultLayout do Routes.razor e so envolve o
    NotFound — toda pagina real declara @layout AppLayout ou AuthLayout.

    Antes carregava o NavMenu do template do MAUI, com links para /counter e
    /weather e um "About" apontando para a documentacao da Microsoft. Nenhuma
    das tres coisas existe no informE, e um clique nelas durante uma
    demonstracao mostraria o template em vez do produto.
*@

<main class="fallback-shell">
    @Body
</main>
